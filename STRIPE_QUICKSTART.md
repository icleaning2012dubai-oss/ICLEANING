# 🛒 Stripe Payment Integration - Quick Start

## ✅ Что было сделано

Интегрирован **Stripe** для обработки онлайн-платежей в корзине покупок.

### Установлено:
- ✅ `stripe` - серверный SDK
- ✅ `@stripe/stripe-js` - клиентский SDK

### Создано:
- ✅ `/api/create-checkout-session` - создание Stripe сессии
- ✅ `/api/webhooks/stripe` - обработка webhook событий
- ✅ `/api/checkout-session` - получение информации о сессии
- ✅ `/checkout/success/page.tsx` - страница успешной оплаты
- ✅ Обновлена страница `/checkout` с поддержкой Stripe
- ✅ Добавлены переводы на 3 языка (EN, RU, AR)

## 🚀 Быстрая настройка

### 1. Получите ключи Stripe

**Для тестирования:**
1. Зарегистрируйтесь на https://dashboard.stripe.com
2. Скопируйте **Test** ключи: https://dashboard.stripe.com/test/apikeys

### 2. Обновите `.env.local`

Откройте файл `.env.local` и замените тестовые значения:

```env
STRIPE_SECRET_KEY=sk_test_ваш_реальный_тестовый_ключ
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_ваш_реальный_тестовый_ключ
```

### 3. Запустите сервер

```bash
npm run dev
```

### 4. Протестируйте оплату

1. Откройте http://localhost:3000
2. Добавьте услугу в корзину
3. Перейдите в Checkout
4. Заполните форму
5. Выберите **"Credit/Debit Card"**
6. Используйте тестовую карту:
   - **Номер:** `4242 4242 4242 4242`
   - **Дата:** любая будущая (например, `12/30`)
   - **CVC:** любые 3 цифры (например, `123`)
   - **ZIP:** любой код (например, `12345`)

## 💳 Тестовые карты

| Сценарий | Номер карты |
|----------|-------------|
| ✅ Успешная оплата | `4242 4242 4242 4242` |
| ❌ Отклоненная | `4000 0000 0000 0002` |
| 🔐 Требует 3D Secure | `4000 0025 0000 3155` |

Полный список: https://stripe.com/docs/testing

## 📁 Структура файлов

```
app/
├── api/
│   ├── create-checkout-session/
│   │   └── route.ts              ← Создание Stripe сессии
│   ├── webhooks/stripe/
│   │   └── route.ts              ← Обработка webhook
│   └── checkout-session/
│       └── route.ts              ← Получение данных сессии
├── checkout/
│   ├── page.tsx                  ← Обновлено с Stripe
│   └── success/
│       └── page.tsx              ← Новая страница успеха
└── locales/
    ├── en.json                   ← Обновлены переводы
    ├── ru.json                   ← Обновлены переводы
    └── ar.json                   ← Обновлены переводы

.env.local                        ← Добавлены Stripe ключи
```

## 🔄 Как это работает

```
1. Клиент → Checkout → Выбирает карту
                ↓
2. POST /api/create-checkout-session
                ↓
3. Stripe создает сессию → возвращает URL
                ↓
4. Redirect → Stripe Checkout (безопасная страница)
                ↓
5. Клиент вводит данные карты
                ↓
6. Успешная оплата → /checkout/success
                ↓
7. Webhook получает событие → можно сохранить в БД
```

## ⚠️ Важно для Production

Перед запуском на production:

- [ ] Замените test ключи на **live** ключи
- [ ] Настройте webhook на production URL
- [ ] Добавьте HTTPS (Stripe требует HTTPS для webhooks)
- [ ] Протестируйте все сценарии оплаты
- [ ] Настройте email уведомления

## 📚 Документация

- [STRIPE_INTEGRATION.md](./STRIPE_INTEGRATION.md) - Полная документация
- [STRIPE_USAGE_GUIDE.md](./STRIPE_USAGE_GUIDE.md) - Примеры кода и кастомизация
- [Stripe Docs](https://stripe.com/docs) - Официальная документация

## 🆘 Проблемы?

### "No API key provided"
→ Проверьте `.env.local` и перезапустите сервер

### Webhook не работает локально
→ Используйте Stripe CLI для перенаправления webhook:
```bash
stripe listen --forward-to localhost:3000/api/webhooks/stripe
```

### Оплата не проходит
→ Убедитесь что используете тестовые карты и test ключи

## 🎉 Готово!

Теперь ваша корзина поддерживает:
- ✅ Оплату наличными при доставке
- ✅ Онлайн-оплату через Stripe
- ✅ Безопасную обработку платежей
- ✅ Мультиязычность

---

**Нужна помощь?** Проверьте полную документацию в `STRIPE_INTEGRATION.md`
