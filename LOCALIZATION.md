# Система локализации HomyClean

## Поддерживаемые языки

- 🇷🇺 Русский (ru) - по умолчанию
- 🇬🇧 Английский (en)
- 🇸🇦 Арабский (ar) - с поддержкой RTL

## Как использовать

### В компонентах

```tsx
import { useLanguage } from '@/app/contexts/LanguageContext';

function MyComponent() {
  const { t, language, setLanguage } = useLanguage();
  
  return (
    <div>
      <h1>{t('hero.title1')}</h1>
      <p>{t('hero.subtitle')}</p>
    </div>
  );
}
```

### Доступные функции

- `t(key: string)` - функция перевода
- `language` - текущий язык ('ru' | 'en' | 'ar')
- `setLanguage(lang)` - изменить язык
- `dir` - направление текста ('ltr' | 'rtl')

## Структура файлов

```
app/
├── locales/
│   ├── ru.json    # Русские переводы
│   ├── en.json    # Английские переводы
│   └── ar.json    # Арабские переводы
└── contexts/
    └── LanguageContext.tsx  # Контекст локализации
```

## Добавление новых переводов

1. Откройте нужный файл в `app/locales/`
2. Добавьте новый ключ и значение:

```json
{
  "myNewKey": "Мой новый перевод"
}
```

3. Используйте в компоненте:

```tsx
{t('myNewKey')}
```

## Особенности

### RTL для арабского языка

Система автоматически применяет RTL (справа налево) для арабского языка:
- Направление текста меняется автоматически
- Иконки отражаются зеркально
- Отступы адаптируются под RTL

### Сохранение выбора языка

Выбранный язык сохраняется в `localStorage` и восстанавливается при следующем посещении.

### Переключатель языков

Переключатель языков доступен:
- В шапке сайта (десктоп и мобильная версии)
- В мобильном меню

## Шрифты

- Латиница: Inter, Plus Jakarta Sans
- Арабский: Tajawal (400, 500, 700)

## Категории переводов

- `nav.*` - Навигация
- `hero.*` - Главный баннер
- `about.*` - О компании
- `services.*` - Услуги
- `stats.*` - Статистика
- `contact.*` - Контакты
- `footer.*` - Футер
- `cart.*` - Корзина
- `breadcrumb.*` - Хлебные крошки
